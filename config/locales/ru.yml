# Files in the config/locales directory are used for internationalization
# and are automatically loaded by Rails. If you want to use locales other
# than English, add the necessary files in this directory.
#
# To use the locales, use `I18n.t`:
#
#     I18n.t "hello"
#
# In views, this is aliased to just `t`:
#
#     <%= t("hello") %>
#
# To use a different locale, set it with `I18n.locale`:
#
#     I18n.locale = :es
#
# This would use the information in config/locales/es.yml.
#
# The following keys must be escaped otherwise they will not be retrieved by
# the default I18n backend:
#
# true, false, on, off, yes, no
#
# Instead, surround them with single quotes.
#
# en:
#   "true": "foo"
#
# To learn more, please read the Rails Internationalization guide
# available at https://guides.rubyonrails.org/i18n.html.

ru:
  activerecord:
    errors:
      messages:
        accepted: нужно подтвердить
        blank: не может быть пустым
        present: должно быть пустым
        confirmation: не совпадает
        empty: не может быть пустым
        equal_to: должно иметь значение, равное %{count}
        even: должно иметь нечетное значение
        exclusion: зарезервированное значение
        greater_than: должно иметь значение большее %{count}
        greater_than_or_equal_to: должно иметь значение большее или равное %{count}
        inclusion: непредусмотренное значение
        invalid: неверное значение
        less_than: должно иметь значение меньшее чем %{count}
        less_than_or_equal_to: должно иметь значение меньшее или равное %{count}
        not_a_number: не является числом
        not_an_integer: не является целым числом
        odd: должно иметь четное значение
        taken: уже существует
        too_long: длина должна быть меньше %{count}
        too_short: длина должна быть больше %{count}
        wrong_length: длина должна быть равна %{count}
        other_than: должно отличаться от %{count}
    attributes:
      email: электронная почта
      username: имя
      password: пароль
      password_confirmation: подтверждение пароля
      consent: согласие
